apiVersion: v1
kind: Service
metadata:
  name: gitlab
  labels:
    app: gitlab
spec:
  type: NodePort
  selector:
    app: gitlab
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30481
    - name: ssh
      port: 22
      targetPort: ssh
      nodePort: 30222
