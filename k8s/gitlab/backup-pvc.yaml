apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-backups
  labels:
    app: gitlab-backup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
