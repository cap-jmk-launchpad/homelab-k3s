# Static PV for warm index on engine NVMe (nvme0n1 @ /srv/homelab/nvme).
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: li-research-warm-index
  labels:
    app: li-research
    app.kubernetes.io/component: warm-index
    machine: engine
spec:
  capacity:
    storage: 250Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: li-research-bulk
  hostPath:
    path: /srv/homelab/nvme/li-research/warm-index
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - engine
