# DEPRECATED — homelab edge is li-native (li-httpd only).
#
# Active routes: k8s/edge/homelab.httpd.toml (+ majico-staging.httpd.toml merge)
# Apply:         scripts/edge-lis-apply.sh
# Policy:         docs/li-native-edge.md
#
# Legacy Caddy config preserved at k8s/edge/deprecated/Caddyfile.legacy for reference.
