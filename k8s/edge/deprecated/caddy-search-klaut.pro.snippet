# Deprecated — pre li-native migration. Routes live in homelab.httpd.toml.
# Do not append bare `search.klaut.pro { reverse_proxy ... }` without manual TLS:
# Caddy 2.6 auto-HTTPS panics when WAN :80 is closed (ACME HTTP-01 fails).
