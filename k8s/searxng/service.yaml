apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: searxng
  labels:
    app: searxng
spec:
  type: NodePort
  selector:
    app: searxng
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30479
