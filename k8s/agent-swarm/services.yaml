apiVersion: v1
kind: Service
metadata:
  name: agents-dashboard
  namespace: agent-swarm
  labels:
    app: agents-dashboard
spec:
  type: NodePort
  selector:
    app: agents-dashboard
  ports:
    - name: http
      port: 9477
      targetPort: http
      nodePort: 30477
