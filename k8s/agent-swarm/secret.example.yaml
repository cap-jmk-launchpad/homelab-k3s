# Template only — do NOT commit real keys. Create the live secret with:
#   ./scripts/k8s-agent-swarm-secret.sh /path/to/li-cursor-agents/.env
#
apiVersion: v1
kind: Secret
metadata:
  name: agent-swarm-secrets
  namespace: agent-swarm
type: Opaque
stringData:
  CURSOR_API_KEY: "replace-me"
  GH_TOKEN: "replace-me"
  SUPABASE_SERVICE_ROLE_KEY: "replace-me"
  SUPABASE_ANON_KEY: "replace-me"
  SUPABASE_DB_URL: "postgresql://postgres:postgres@192.168.10.41:54322/postgres"
