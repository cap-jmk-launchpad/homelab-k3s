apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-swarm-config
data:
  LI_HOST_ROOT: "/home/s4il0r/li-langverse"
  # In-cluster PostgREST (same namespace DNS)
  SUPABASE_URL: "http://postgrest:54321"
  LI_SDK_MAX_CONCURRENT: "2"
  LI_SWARM_MAX_PARALLEL: "2"

