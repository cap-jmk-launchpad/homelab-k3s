apiVersion: apps/v1
kind: Deployment
metadata:
  name: obsevia-chat-frontend
  namespace: obsevia-chat-staging
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: blackpearl
