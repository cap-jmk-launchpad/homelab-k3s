apiVersion: v1
kind: Service
metadata:
  name: cwe-mirror
  labels:
    app: cwe-mirror
spec:
  type: NodePort
  selector:
    app: cwe-mirror
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30483
